for i in range(0, 1002):
    # σύμφωνα με τη θεωρία πρέπει να βρόυμε έναν αριθμό i τέτοιον ώστε αν πολλαπλασιαστεί με το 809 και γίνει mod με
    # το 1001, να βγάζει το αποτέλεσμα = 1.
    x = (809 * i) % 1001
    if x == 1:
        print(f'To antistrofo stoixeio einai to: {i}\n')
